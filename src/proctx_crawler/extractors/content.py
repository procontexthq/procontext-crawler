"""Raw HTML extraction wrapper for pipeline consistency."""

from __future__ import annotations


def extract_html(html: str) -> str:
    """Return the HTML content as-is. Minimal wrapper for consistency."""
    return html
